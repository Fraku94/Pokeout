<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pokeout.pokeout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginRegister.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginRegister.RegisterActivity" />
        <activity android:name=".Profil.ProfilActivity" />
        <activity android:name=".CategoryAdd.CategoryAddActivity" />
        <activity android:name=".UsersInCategory.UsersInCategoryActivity" />
        <activity android:name=".UserDescryption.UserDescryptionActivity" />
        <activity android:name=".Chat.ChatActivity" />
        <activity android:name=".CategoryDescryption.CategoryDescryptionActivity"></activity>
    </application>

</manifest>